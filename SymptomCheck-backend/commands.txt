to run a service
cd nameservice
mvnw.cmd spring-boot:run -DskipTests


to build a image
docker-compose up --build



when you update the client in keycload you should  delete the volume of the container by:
docker-compose down -v
and :

docker-compose up --build



unit test by this command  you should chage the name of the service appointmentservice :
mvnw.cmd pitest:mutationCoverage -DtargetTests=com.symptomcheck.appointmentservice.unit.*