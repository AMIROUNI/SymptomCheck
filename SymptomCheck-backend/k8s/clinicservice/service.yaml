apiVersion: v1
kind: Service
metadata:
  name: clinicservice
  namespace: symptomcheck-backend
spec:
  type: ClusterIP
  selector:
    app: clinicservice
  ports:
    - port: 80
      targetPort: 8085
